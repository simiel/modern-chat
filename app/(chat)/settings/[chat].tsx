import Text from '@/components/Text';
import React from 'react';
import { SafeAreaView } from 'react-native';

const Settings = () => {
  return (
    <SafeAreaView>
      <Text style={{ color: 'red' }}>Chat Settings</Text>
    </SafeAreaView>
  );
};

export default Settings;
